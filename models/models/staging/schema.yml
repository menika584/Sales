version: 2

models:

  - name: stg_employees
    columns:
      - name: employee_id
        tests:
          - not_null
          - unique

  - name: stg_departments
    columns:
      - name: department_id
        tests:
          - not_null
          - unique
  - name: stg_order
    columns:
      - name: order_id
        tests:
          - not_null
          - unique
      - name: student_id
        tests:
          - not_null
      - name: course_id
        tests:
          - not_null

